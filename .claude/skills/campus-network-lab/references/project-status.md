# Ảnh chụp trạng thái dự án (2026-09-20)

**Đây là snapshot theo thời điểm, không phải sự thật hiện tại.** Nó tổng hợp từ: bảng tiến độ 05/09, `.opencode` summaries 18–19/09, `HuongDan/PhucHoi…` 19/09, `git status`, và kiểm tra cục bộ ngày 20/09. Mọi mục "live" phải kiểm lại trước khi hành động hoặc báo cáo. Cập nhật file này (ghi ngày) cuối phiên có thay đổi đáng kể, thay vì viết nhật ký vào `SKILL.md`.

## Kiểm tra cục bộ đã làm (20/09, đáng tin nhất)

- `.unl`: 67 node, 100 network, 51 `config="1"` = 51 config nhúng; `unl_tool.py validate` **ĐẠT**.
- `unl_tool.py drift`: **12/51** config nhúng lệch file `configs/` — Brand-FW 37/38/39, SW55–60, SwitchBrand 62/63/64 (lệch nội dung); Core-SW1/2, vEdge1-S400 (31), Switch61 (khác EOL/khoảng trắng). Chưa quyết định bản nào chuẩn.
- Working tree có thay đổi **chưa commit**: `Campus-OVS-restore.sh` (bootstrap VLAN 99 theo cây), 6 file `systemd/ovs-nodes/*.env`, `campus_switch_13.py` (~650 dòng đổi), `campus-ovs-restore.service`, `Core-SW1/2 config.cfg` (chỉ newline), `Web-Server/setup.sh`. Chưa xác định đã deploy lên node nào.
- File chưa track: `HuongDan/PhucHoiKetNoi_2_vEdge_Site100.txt` (đáng commit), `inspect_dist_console.py`, `n68_tagc.png` (ảnh chụp màn hình, chưa rõ còn cần), `.opencode/skills/campus-network-lab/summaries/`.

## Đã hoàn tất (theo ghi nhận, kiểm live khi cần)

- Thiết kế + bảng IP/VLAN/ASN; Core IOL, FW-ASAv HA + ASDM, OSPF FW↔Core; DMZ/Farm; Syslog (Kiwi).
- DHCP campus end-to-end (15/09): VPC14+19 nhận IP `10.1.10.100/.101` sau khi sửa app Ryu (3 root cause, xem `sdn-ryu-ovs.md`).
- SD-WAN: 3 controller, PKI, 8 vEdge lên fabric (06/09: 8/8 reachable), BGP underlay; 19/09 khôi phục 2 vEdge S100 bằng add whitelist 3 controller.
- Chi nhánh: S400 verify DHCP; S200/S300 dán IOL tay (commit `4ad720f` ghi hoàn tất — xác minh live).
- NOC monitoring app Ryu (`campus_noc_monitor.py`) đã triển khai (commit `12282f1`).

## Cập nhật 20/09/2026 (cuối ngày, đã kiểm chứng live) — storm đã dứt điểm

- **6/6 OVS kết nối Ryu, ổn định, 11 link Dist UP.** Đã kiểm chứng: restart Ryu (6/6 lên trong ~15 s); reboot từng Access 68/66/70/69 (~2,5 phút); reboot Dist-SW2 với 4 link Access cắm (~105 s); **dừng hẳn Dist-SW1**: 5 switch còn lại giữ kết nối, bật lại thì tự phục hồi 6/6 (~2 phút). pps cao nhất mọi tap trong bài thử cuối: 365.
- **Nguyên nhân gốc (đã chứng minh):** OVS **in-band control** tự cài flow ẨN action `NORMAL` cho ARP khi chưa kết nối controller (log `in_band|WARN|br0: cannot find route for controller`) → flood ARP broadcast của controller ra mọi cổng → storm ~200.000 gói/s mỗi khi một OVS boot. Đã tắt: `other_config:disable-in-band=true` trong script restore của cả 6 node. Chi tiết + cách nhận biết: `sdn-ryu-ovs.md`.
- **Các nguyên nhân phụ đã sửa:** flow NORMAL bootstrap; `tag=99` của patch chỉ có tác dụng với NORMAL (Access dùng flow cố định cookie `0xba5f` priority 45000 gán/bóc tag; Dist dùng `br-mgmt.99` nên giữ nguyên); `OFPFC_DELETE` không xóa được TREE-BLOCK (đổi `DELETE_STRICT`); `_flush_output_port` xóa nhầm flow cố định của Access (giới hạn cookie 0); guard VLAN 99 ở ens8 chặn sw5 về controller (đã gỡ — Access là nút lá nên không có vòng).
- **App Ryu (node 9, md5 `8d4b2e53…`, khớp repo):** `TCP_USER_TIMEOUT` 15 s; failover chỉ khi ping sw5 thất bại 3 lần **và sw8 đang kết nối**; xóa bootstrap theo cookie `0xba5e`. Script đang chạy trên 6 OVS: `configs/01-Site100-Campus/ovs-deployed/` (md5 Access `8eeb6adb…`, Dist `a84768d6…`); bản Access cũ ở `backup.md`.
- **Còn mở:** (1) ~~Core-SW1/2 + Farm~~ ĐÃ đồng bộ live → `configs/` (20/09: prune VLAN 99 nhánh phụ, `spanning-tree vlan 99` priority 8192/12288); `.unl` trong repo CHƯA embed lại (`.unl` chỉ đồng bộ một chiều host 1 → repo) nên `unl_tool.py drift` sẽ báo lệch 3 node này. (2) 12/51 config nhúng lệch `configs/` (chi nhánh). (3) Mật khẩu OVS (VNC) chưa biết — thao tác bằng ghi đĩa offline. (4) Repo chưa commit. (5) Nên thử reboot **cả host EVE** (cold start toàn lab) để chắc thứ tự khởi động.

## Cập nhật tối 20/09/2026 — DHCP + failover data-plane (để tiếp tục 21/09)

**Trạng thái cuối phiên (đã kiểm chứng):** 6/6 OVS kết nối Ryu, 4 link dự phòng Dist-SW2↔Access UP, Ryu ở chế độ bình thường (md5 app `8d4b2e53…`), node 5 và 8 đều chạy, 8 VPC Site 100 (14–21, node đã start) giữ IP `.100/.101` từ DHCP-Server node 72, ping gateway/DHCP-Server/liên VLAN 3/3.

**Đã kiểm chứng DHCP (bình thường):** 8/8 VPC xin được IP đúng scope/gateway (VLAN10 `10.1.10.100/.101`, VLAN20 `.20.`, VLAN30 `.30.`, VLAN40 `.40.`; DHCP server `10.1.90.10`), DORA đủ, ping liên VLAN OK.

**Bài thử DHCP khi dừng hẳn Dist-SW1 (đã làm, KẾT QUẢ CHƯA ĐẠT):**
- Ryu failover đúng (~30 s), 5 switch giữ kết nối, không storm (pps cao nhất ~174).
- **0/8 VPC xin lại được IP** (`ip dhcp -r` rồi `ip dhcp`); VPC14→10.1.90.10 = 0/3; ping gateway 3/3; liên VLAN 2–3/3.
- **Giả thuyết (CHƯA kiểm chứng):** `_apply_failover_switch` (sw8) chỉ mở dữ liệu `ens4-7` + `ens9` (→Core-SW2); `ens10` (→Core-SW1) vẫn chặn dữ liệu (L2_TREE_BLOCK). Core-SW1 mất L2 tới VLAN 10–40 mà vẫn thấy SVI up → reply DHCP relay từ server (gateway VRRP ở Core-SW1) tới giaddr Core-SW2 bị mất. Đề xuất sửa: trong failover mở thêm dữ liệu trên `ens10` của sw8 (`_remove_tree_block(dp, ens10, include_vlan99=False)` + thêm VLAN 10–40,90 vào `vlan_ports[8]`; deactivate đã re-block qua `_install_tree_block_drops`). Không tạo vòng L2 vì Core1↔Core2 là L3 (Po10); cần xác nhận bằng `show vrrp brief` + `show ip arp` trên hai Core NGAY LÚC failover (chưa làm).

**Sự cố khi failback (chưa rõ nguyên nhân):** sau khi bật lại node 5 (lúc 8 VPC đang chạy và đã có flow unicast do failover), Ryu chạy `RECOVERY` rồi **cả 6 switch mất kết nối cùng lúc**, Ryu tự failover lại và không tự phục hồi. Không phải storm (chỉ vài gói ARP); controller vẫn trả ARP reply tới sw8.ens10 nhưng không tới được các switch ⇒ flow VLAN 99 trên sw8 hỏng/kẹt sau RECOVERY (OVS `secure` giữ flow hỏng, Ryu không vào sửa được). Lần failback trước (`final_test`, lúc chưa có VPC chạy) thành công. Cần thêm log chi tiết trong `_failover_deactivate` (thứ tự flow_mod, flow VLAN 99 thực tế trên sw8 trước/sau) rồi tái hiện có kiểm soát.
**Cách phục hồi đã dùng (hiệu quả):** cắt tap `vunl6_8_1..4` (Dist-SW2↔Access) → restart `campus-ryu` → nếu sw8 vẫn không lên thì dừng/khởi động node 8 (~3 phút, script trên đĩa nạp bootstrap sạch) → chờ 6/6 → bật lại 4 link từng cái, đo pps, ngưỡng >3000 gói/3 s thì cắt lại.

**Việc tiếp theo (thứ tự đề xuất):**
1. Sửa failover data-plane (ens10 sw8) + thêm log RECOVERY; deploy app (`deploy_app`: gz+b64 qua serial node 9 → md5 → `cp` → `systemctl restart campus-ryu`); cần quyền chạy lệnh restart/ghi đĩa như đã được cho.
2. Thử lại có kiểm soát: dừng node 5 → đo DHCP + xem VRRP/ARP trên 2 Core → bật node 5 → xác nhận failback 6/6 (làm khi VPC đang chạy để lặp đúng điều kiện lỗi).
3. Thử cold-start cả lab (host EVE) để kiểm thứ tự boot; commit repo (đang có nhiều thay đổi chưa commit: app, `ovs-deployed/`, `backup.md`, Core/Farm config, skill `.claude/`).
4. Nếu còn thời gian: embed lại config Core-SW1/2 + Farm vào `.unl` (theo quy tắc host 1 → repo), 12/51 config nhúng lệch chi nhánh, báo cáo Chương 4/5 (thêm nguyên nhân storm in-band + kết quả kiểm thử).

**Lưu ý vận hành đã học hôm nay:** (a) khi dừng sw8 hoặc sw5, đường quản trị đứt tạm — Ryu chỉ failover nếu sw8 đang kết nối; (b) đọc đĩa node đang chạy bằng guestfish có thể ra dữ liệu cũ — dừng node rồi đọc; (c) `vpcs`: `ip dhcp -r` rồi `ip dhcp`; console VPC = `33536+id`; (d) lệnh dài qua `paramiko exec` cần bọc bằng nháy đơn để tránh lỗi quoting; (e) script tạm nằm ở scratchpad phiên (`eve_ssh.py`, `console9.py`, `ios.py`, `deploy_app.py`, `node_patch.sh`…) có thể mất — dựng lại theo `console-automation.md` và `sdn-ryu-ovs.md`.

## Cập nhật 21/09/2026 — ONOS trên node 9 (đã kiểm chứng live)

- **Đã cài ONOS 2.7.0** (Java 11.0.19, `/opt/onos`, user `onos`, `systemd onos.service`, **chưa `enable`**) chạy **song song Ryu**: OpenFlow `6654` (đặt qua `/opt/onos/config/component-cfg.json`, `openflowPorts`), REST/GUI `8181`, Karaf SSH `8101`; app `openflow`, `gui2`, `drivers`, `lldpprovider`, `hostprovider`. Ryu vẫn giữ `6653`/`8080`, 6/6 OVS vẫn nối Ryu; ONOS `devices: []` (chưa quản lý switch nào). Log cài: `/var/log/onos-install.log`; log ONOS: `/opt/onos/apache-karaf-4.2.9/data/log/karaf.log`.
- **Ra Internet của node 9 (Cloud-NAT `ens6`, cầu thẳng vào `pnet0`, IP DHCP 10.215.28.x):** trước đó hỏng vì default route tĩnh `via 10.1.99.1 dev ens3` (metric 0) thắng route DHCP `ens6` (metric 100) → reply về `ens6` bị `rp_filter=1` loại. Đã sửa **tạm thời (mất khi reboot)**: `ip route del default via 10.1.99.1 dev ens3; ip route add default via 10.1.99.1 dev ens3 metric 200`. Chưa ghi vào netplan/`Campus-Cloud-DHCP.sh`.
- **Serial node 9 dùng cổng ngẫu nhiên** (`-serial telnet:0.0.0.0:0`): tra bằng `ss -tlnp | grep pid=<qemu>`; `33536+9` là VNC. Khi ONOS đang nạp, serial rất chậm → kiểm tra từ host EVE qua LAN (`10.215.28.59`).
- **Phát hiện chặn cutover ngây thơ (đọc `/stats/flow/<dpid>` của Ryu, 21/09):** table-miss trên OVS là `priority=0 → OUTPUT:CONTROLLER` (KHÔNG phải NORMAL); flow unicast do Ryu học là vĩnh viễn (`idle=0`); broadcast/unknown (ARP, DHCP) đều phải qua packet-in của controller. Access dùng flow tường minh `PUSH_VLAN/POP_VLAN` (access port `tag=` chỉ có tác dụng với NORMAL). ⇒ dừng Ryu → host mới/ARP/DHCP mất; bật ONOS `fwd` → không gán/bóc VLAN ở cổng access → sai VLAN. ONOS thay Ryu cần app L2-VLAN thật (VPLS + interface config theo cổng), chống vòng và failover phải làm lại. ONOS mặc định còn xoá "extraneous flows" của Ryu (cần `allowExtraneousRules=true`). Chưa thay đổi gì trên OVS/Ryu.
- **Hướng 1 ĐÃ TRIỂN KHAI (21/09, kiểm chứng live):** ONOS làm GUI/giám sát, Ryu vẫn chuyển tiếp. Trên cả 6 OVS: `ovs-vsctl set-controller br0 tcp:10.1.99.10:6653 tcp:10.1.99.10:6654` (gõ qua VNC, đăng nhập user `eve`, mật khẩu ghi trong file credentials cục bộ, sudo cùng mật khẩu). ONOS: `allowExtraneousRules=true`, `HostLocationProvider requestInterceptsEnabled=false, requestArp=false`, không bật `fwd`. Kết quả: ONOS 6/6 device available, 18 link một chiều (= 8 Dist↔Access + Dist-SW1↔Dist-SW2), Ryu vẫn 6/6, flow Ryu giữ nguyên (chỉ +2 flow LLDP ưu tiên 40000/switch), pps cao nhất ~206, 8 VPC (14–21, người dùng đã tắt rồi tôi bật lại) DHCP 8/8 DORA + ping gateway/DHCP-Server/liên VLAN 7/7. GUI: `http://10.215.28.59:8181/onos/ui/` (onos/rocks). ONOS KHÔNG thấy host (đã tắt hostprovider intercept) và không thấy Core (IOL không OpenFlow).
- **Đã lưu bền (21/09):** `set-controller … 6653 … 6654` đã nằm trong script restore trên đĩa cả 6 OVS (md5 Access `39055868…`, Dist `9eb0685d…`; bản cũ `/root/Campus-OVS-restore.sh.bak-onos`) và trong repo (`ovs-deployed/*.sh`, `Campus-OVS-restore.sh`). Kiểm chứng: reboot Access-SW2 → Ryu 6/6, ONOS device 42 quay lại, VPC20/21 DHCP + ping OK, pps ~204.
- **Cố định ONOS vào IP tĩnh (21/09) — THỬ `cluster.json` THẤT BẠI, đã hoàn tác:** ghi `/opt/onos/config/cluster.json` dạng `{name,node{id,ip,port},clusterSecret,storage[{id,ip,port}]}` với IP `10.1.99.10` → ONOS đọc file (member `10.1.99.10` activated) nhưng Atomix lặp `Failed to locate management group via bootstrap nodes… ensure partition groups are configured` >7 phút, REST/6654 không lên. Định dạng đúng của ONOS 2.7 cần tra thêm (có thể thiếu khối partition group). Đã khôi phục dữ liệu từ `/opt/onos-backup/data-20260921-095244`, bỏ `cluster.json` (lưu ở `/opt/onos-backup/*failed*`) → ONOS lên lại ~35 s, cluster id vẫn `10.215.28.59`, 6 device/18 link, Ryu 6/6. Được giữ lại: `/opt/onos/config/component-cfg.json` (openflowPorts 6654, allowExtraneousRules, tắt host intercept) nên thiết lập không mất khi xóa dữ liệu ONOS. Hướng thử tiếp: biến `ONOS_NIC=10.1.99.*` trong unit (chưa xác minh bản này có hỗ trợ) + xóa `data` (id node đổi); hoặc tra đúng `cluster.json` cho 2.7; lưu ý nếu rút Cloud-NAT thì ONOS sẽ tự chọn `10.1.99.10` nhưng dữ liệu cũ gắn `.59` → phải xóa `data`.
- **IP guard cho ONOS (21/09, đã kiểm chứng):** `/opt/onos/bin/onos-ip-guard.sh` chạy qua `ExecStartPre=+` của `onos.service` (`After=campus-cloud-dhcp.service`): so tập IPv4 (`ip -4 -o addr show scope global`) với `/opt/onos/.last-ips`; khác → chuyển `data` sang `/opt/onos-backup/data-auto-<ts>` (giữ 2 bản), tạo `data` mới, ghi IP mới; lần đầu chỉ khởi tạo. Thử: giả lập IP đổi (`9.9.9.9`) → guard xóa data → ONOS tự lên ~1,5 phút, 6 device/18 link, `component-cfg.json` giữ thiết lập, Ryu 6/6, pps ~241. Ghi log ở `/var/log/onos-install.log`. Cluster id ONOS vẫn theo IP DHCP (`10.215.28.59`) khi Cloud-NAT còn cắm; khi rút, guard tự dựng lại với `10.1.99.10`. `onos.service` ĐÃ `enable` lúc boot (21/09). **Đã thử reboot node 9 (11:21 UTC 21/09):** guard ghi `IP khong doi`, ens6 nhận lại `10.215.28.59`, ~90 s sau lệnh reboot Ryu 6/6 + ONOS REST 200 + 6 device/18 link, app ACTIVE, pps ~56, VPC 14–21 DHCP 8/8 DORA. **Đã thử RÚT Cloud-NAT thật (21/09, `ip link set vunl6_9_3 nomaster` trên host rồi reboot node 9):** `ens6` không IP → guard log `IP doi '10.1.99.10 10.215.28.59 ' -> '10.1.99.10 '` xóa data → ONOS tự lên ~1 phút 45 s với cluster id `10.1.99.10` READY, 6 device/18 link, app + thiết lập giữ nguyên, Ryu 6/6, node 9 ping PC-Management `10.1.99.50`, VPC 14–21 DHCP 8/8. Cắm lại (`ip link set vunl6_9_3 master pnet0` + `systemctl restart campus-cloud-dhcp`): `ens6` nhận IP MỚI `10.215.28.57` (khác `.59`), ONOS vẫn chạy id `10.1.99.10`; host tới được `.57` nhưng laptop không (nên dùng PC-Management → `10.1.99.10:8181`). **Route Internet sửa tạm (metric ens3 200) đã MẤT sau reboot node 9** → node 9 lại không ra Internet qua ens6 (ping 8.8.8.8 mất gói) cho tới khi ghi bền vào cấu hình. Chưa đo thời gian gián đoạn dữ liệu trong lúc controller tắt (OVS `secure` giữ flow cũ nhưng ARP/DHCP mới cần controller).
- **Còn chưa làm (ONOS):** `enable` onos lúc boot; đổi mật khẩu ONOS. Quan sát từ PC-Management (node 73, `10.1.99.50`): `http://10.1.99.10:8181/onos/ui/` (mạng đã thông, chưa mở thử trình duyệt trong PC).
- **Công cụ VNC (scratchpad, có thể mất):** `vncdotool` 1.3.0 cài sẵn trên máy; VNC ở host EVE cổng = 5900 + số `-vnc :N` (tra bằng `ss -tlnp | grep pid=<qemu>`): 66→33602, 68→33604, 70→33606, 69→33605, 5→33541, 8→33544 (đổi mỗi lần start). Tránh `&`, dùng Shift map riêng; `.`/`/` gõ trực tiếp; nên gõ → chụp màn hình → Enter.
- **Chưa làm / cần quyết định:** cutover 6 OVS từ Ryu sang ONOS (Ryu đang gánh chống vòng L2 + cây VLAN 99 nên không đổi cổng là xong), `enable` ONOS lúc boot, đổi mật khẩu mặc định ONOS, chặn `ens6` khỏi 8181/8101/8080 (đang mở ra LAN `10.215.28.0/24`).

## Đang dở / vấn đề mở (ưu tiên)

1. ~~6 OVS không kết nối Ryu / storm~~ — **ĐÃ GIẢI QUYẾT 20/09** (xem mục cập nhật trên).
2. Web-Server (node 22), Mail, Syslog hoàn thiện; kịch bản demo ASDM.
3. Chi nhánh S200/S300: xác minh IOL đã dán tay + DHCP; 12/51 config nhúng lệch `configs/`.
4. Kiểm thử tích hợp (ma trận test: DHCP từng site, FW failover, cắt 1 màu WAN → BFD, mất controller), báo cáo Chương 4/5, slide, video demo.
5. Rủi ro vận hành: host EVE reboot mất route `10.9.1.0/24 via <Switch61>`; whitelist controller mất khi reboot controller; Switch61 đổi IP DHCP; chưa thử cold-start cả lab.

## Cách cập nhật snapshot

Ghi: ngày, việc đã làm + **bằng chứng** (lệnh + kết quả), việc chưa làm, blocker, file liên quan. Không ghi mật khẩu, không ghi md5/serial nếu có thể tính lại từ file. Xoá mục đã lỗi thời thay vì để chồng chéo (skill cũ lặp 3 lần một bảng md5 mâu thuẫn nhau — đừng lặp lại).
