*.iso
*.img

# Agent skill/config cục bộ (chỉ đẩy skills/ lên git, ẩn cấu hình nhạy cảm)
.opencode/*
!.opencode/skills/
.codex/


# Bộ cài nhị phân (tải tay, không commit)
tools/*.exe

# LaTeX build artifacts (giữ lại .tex và .pdf)
*.aux
*.log
*.nav
*.out
*.snm
*.toc
*.fls
*.fdb_latexmk

# File khoa tam cua Word/Office khi dang mo tai lieu
~$*
